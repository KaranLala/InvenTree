name: FZ Frontend Build

on:
  push:
    branches: [production]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: fz-frontend-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: yarn
          cache-dependency-path: src/frontend/yarn.lock
      - name: Install dependencies
        run: cd src/frontend && yarn install --frozen-lockfile
      - name: Extract, compile translations and build frontend
        # `extract` re-scans source so newly added/changed strings land in the
        # catalogs; without it `compile` reads stale .po files and untranslated
        # strings leak into the UI as lingui hash IDs (e.g. "UTVshK").
        run: cd src/frontend && yarn run extract && yarn run compile && yarn run build
      - name: Write SHA version file
        run: echo "$GITHUB_SHA" > src/backend/InvenTree/web/static/web/.vite/sha.txt
      - uses: actions/upload-artifact@v7
        with:
          name: frontend-build
          path: src/backend/InvenTree/web/static/web
          include-hidden-files: true
          retention-days: 30
